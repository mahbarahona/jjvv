<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JJVV</title>
        <style>
        :root{
            --font-size:16px;
            --color-primary:#30E3CA;
            --color-primary-ligth:#1DB9C3;
            --color-primary-shade:#11999E;

            --color-dark:#40514E;
            --color-light:#E4F9F5;
        }
        html{
            font-size: var(--font-size);
        }
        *{
            margin: 0;
            padding: 0;
            list-style: none;

        }
        body{

            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100vh;
            background-color: var(--color-primary-ligth);
            font-family: Arial, Helvetica, sans-serif;
            color: rgba(0,0,0,0.8);
        }

        footer{
            font-size: 0.8rem;
            text-align: center;
            padding: 0.7rem;
        }
        .header{
            position: fixed;
            top: 0;
            width: 100%;

            display: flex;
            flex-direction: column;
            align-items: center;
            padding: .5rem;
            background-color: var(--color-primary);
            box-shadow: 0px 0px 15px var(--color-primary-ligth);
        }
        .header__title{
            font-weight: bold;
        }
        .header__nombre-organizacion{

        }



        main{
            display: -webkit-flex;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            padding: 4rem 1rem 1rem 1rem ;

        }
        @media (min-width:600px){
            main{
                max-width: 600px;
                margin-left: auto;
                margin-right: auto;
            }
        }

        .ubicacion .btn{
            background-color: royalblue;
            color: white;

        }
        .ubicacion__direccion{
            font-size: 0.8rem;
        }

        .telefonos{
            display: flex;
            flex-direction: column;
            gap: 1.3rem;
        }


        .horario__horas p{
            text-decoration: underline;
            margin-bottom: .5rem;
        }
        .horario__horas ul{
            font-size: 0.9rem;
        }

        .horario__content{
            display: flex;
            gap: 3rem;
        }

        .ubicacion__direccion{
            background-color: rgba(0,0,0,0.03);
            padding: 1rem;
            
            border-radius: 4px;
            font-size: 1.1rem;
        }

        .ubicacion__direccion--comuna{
            font-weight: bold;
            font-size: 0.7rem;
            text-transform: uppercase;
        }

        .telefonos__numero{
            font-size: 0.8rem;
        }
        .telefonos__cta{
            font-size: 1rem;
        }
        .btn{
            border-radius: 40px;
            min-height: 60px;
            border: none;
            display: block;
            width: 100%;
            transition: all .3s ease-in-out;
            background-color: royalblue;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
            color: white;
            font-size: 1rem;
        }

        .btn:active{
            transform: scale(0.95);
        }

        .btn.jjvv{
            background-color: var(--color-primary);
            color: var(--color-dark);
        }
        .btn.bomberos{
            background-color: #EC255A;
            
        }
        .btn.carabineros{
            background-color: rgba(0, 111, 39, 1);
            color: white;

        }
        .card{
            display: flex;
            flex-direction: column;
            gap: 1rem;

            border-radius: 4px;
            background-color: white;
            padding: 1rem;

            box-shadow: 0px 0px 14px rgba(0,0,0,0.1);
        }

        .cerrado{
            color: rgba(0,0,0,0.6);
            font-size: 0.9rem;
            margin-left: .3rem;
            text-decoration: line-through;
        }
    </style>
</head>
<body>
        {{{body}}}
        
</body>
</html>